/*Юзер вводить розмір масиву. Заповнити новостворений масив рандомними (ЦІЛИМИ) значеннями та порахувати їх середнє арифметичне. Вивести цей масив на екран, та результат розрахунку.*/

let arr = [];
let x = prompt("Введите размер массива")
let sum = 0;
let result;
for ( i = 0; i < x; i++ ) {
    arr.push( Math.round( Math.random() * 100 ));
    sum +=arr[i];
}   alert(arr);
    result = sum / x;
     alert(result);
 